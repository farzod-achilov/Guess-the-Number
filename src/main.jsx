import React from "react";
import ReactDOM from "react-dom/client";
import GuessTheNumber from "./companets/game/Game";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GuessTheNumber />
  </React.StrictMode>
);
